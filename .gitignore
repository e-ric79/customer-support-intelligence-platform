__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Trained artifacts and data — regenerated by the training scripts,
# not committed (they're derived, not source)
backend/model_artifacts/*.pkl
backend/model_artifacts/*.json
training/data/*.csv
cd.yml
